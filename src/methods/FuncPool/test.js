import { expect } from 'chai';
import FuncPool from './index';

export default () => (
  describe('FuncPool', () => {
    it('FuncPool 实例拥有四个属性，updatePool、update、autoRun、removeFromAutoRun');
    it('updatePool');
    it('update');
    it('autoRun');
    it('removeFromAutoRun');
  })
);
